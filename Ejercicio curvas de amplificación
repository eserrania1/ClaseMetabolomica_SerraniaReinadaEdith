install.packages("pacman") #Es un paquete que llama otros paquetes y si no están los instala
library("pacman") #esta función llama al paquete instalado
#vamos a llamar una función que es específica del paquete
p_load("ggplot2",#sirve para graficar
       "dplyr", #facilita el manejo de datos
       "vroom") #llamar repositorios
Datos_curva<-vroom(file="https://raw.githubusercontent.com/ManuelLaraMVZ/resultados_PCR_practica/refs/heads/main/Amplif_grupo1_17022025.csv")
head(Datos_curva) #solo para que me de los seis primeros datos y comprobar que está bien tu tabla
#Cada columna de la tabla es una gráfica diferente
#A1 me tocó
#El nuevo objeto va a tener una asignación a partir de la base de datos (en este caso datos curva)

Curva_1<-Datos_curva %>% 
  select("Cycle", "A1") #se puede poner con el nombre de la columna (Cycle, A1) o con el número de columna (1, 2)
Curva_1
Gráfica_Curva<-ggplot(Curva_1,
                      mapping = aes(x = Cycle,
                                    y = A1))+
  geom_point()+
  geom_line(color="red",
            linewidth=1.2)
#ggplot es para graficar, te pide de donde va a sacar los datos (null) y luego el mapping es que datos quieres en cual eje
#luego tienes que escojer el tipo de gráfica (geom_point es gráfica de dispersión)
Gráfica_Curva
#queremos agregar una línea que una los puntos, para eso es geom_line
# labs es de labels
Gráfica_curva_2<-Gráfica_Curva+
  labs(x="Ciclos",
       y="Fluorescencia",
       title="Creado por: Edith Serranía Reinada",
       subtitle= "Gráfica A1",
       caption= "Ejercicio 3")+
  theme_classic()
Gráfica_curva_2
